{{!-- this is the button for Devour Burger or put back burger --}}
<li>
    {{name}}
    {{!-- devour status is set, when user click on a button in index.handlebar --}}
    <button class="change-devour" data-id="{{id}}" data-devour="{{devour}}">
        Devour Burger
    </button>
</li>